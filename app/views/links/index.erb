<h1> Links </h1>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL:   <%= link.url %>
      Tag: <%= link.tags %>
    </li>
  <% end %>
</ul>

<a href="/links/new" >Create new link</a>
